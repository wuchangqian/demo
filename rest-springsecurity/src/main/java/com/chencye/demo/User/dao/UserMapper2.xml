<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chencye.demo.User.dao.UserMapper2">
    <select id="list" resultType="com.chencye.demo.User.bean.User">
        SELECT `user`.id, `user`.username, `user`.`password`, `user`.`status` FROM `user`
    </select>
</mapper>
